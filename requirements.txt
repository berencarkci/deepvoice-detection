# Deepvoice-Detection — proje .venv için bağımlılıklar
# Kurulum: python -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt

numpy>=2.0
pandas>=2.0
scipy>=1.11
scikit-learn==1.8.0   # kaydedilmiş RF/SVM modelleriyle aynı sürüm (sürüm uyuşmazlığı uyarısını önler)
joblib>=1.3
matplotlib>=3.8

# CNN eğitimi (CUDA sürümü sisteminize göre https://pytorch.org adresinden seçilebilir)
torch>=2.2
torchvision>=0.17
